// dependencies
import React from 'react';
// components
import { SignIn, SignUp } from '../auth';
// css
import './style.css';

export const App = () => {
    const [user, setUser] = useState(null);
    return (
        <div className='app'>
        </div>
    )
};